# Copy this file to .env.local for local development
# Keep secrets out of version control by using .env.local or Vercel project settings

# Local API base URL; leave empty to use Vite proxy pointing to http://127.0.0.1:8000
VITE_API_BASE_URL=

# Optional custom branding name used by the UI
VITE_APP_NAME=Artium
